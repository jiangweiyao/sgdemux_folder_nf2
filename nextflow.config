profiles {
    local {
        includeConfig 'config/local.config'
    }

    docker {
        docker.enabled = true
        process.scratch=false
        includeConfig 'config/docker.config'
    }
}
